# Node 22 disallows `--disable-warning=*` in `NODE_OPTIONS`.
# Some environments have `pnpm config set node-options "... --disable-warning=..."`,
# which breaks `pnpm dev` / `pnpm build` for this example unless overridden.
node-options=
