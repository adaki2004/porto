# Node 22 disallows `--disable-warning=*` in `NODE_OPTIONS`.
# Some environments set `pnpm config set node-options "... --disable-warning=..."`,
# which breaks pnpm scripts and repo hooks unless overridden.
node-options=
